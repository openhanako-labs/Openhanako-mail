# Contributors

## 维护者

| 账号 | 角色 |
|---|---|
| [@Yuexiye](https://github.com/Yuexiye) | 维护者 / 发布者 |

## AI 协作者

| 账号 | 角色 |
|---|---|
| [@HanaAgent](https://github.com/HanaAgent) | AI 协作者（参与设计、实现与文档） |

## 关于 Contributor 列表

GitHub 的 Contributors 面板**不是一份可以邀请的名单** —— 它由提交记录自动算出：
只要某个账号作为提交的 `author` 或 `Co-Authored-By` 出现，就会进列表。

「组织成员」「仓库协作者（有写权限）」和「Contributors」是三件不同的事，别混淆。

### 为什么列表里会有一个 `claude`

两个 v1 时代的提交带了署名尾注：

```
Co-Authored-By: Claude <noreply@anthropic.com>
```

GitHub 把它算作共同作者，所以面板上会显示一个形如「克洛德·克洛德」的条目。
那是历史记录，此后的提交改用 `@HanaAgent` 署名。

### 约定

AI 参与写作的提交，在提交信息末尾加一行：

```
Co-Authored-By: HanaAgent <313794804+HanaAgent@users.noreply.github.com>
```

> 邮箱必须是该账号在 GitHub 上登记的 noreply 地址（`<user id>+<login>@users.noreply.github.com`），
> 否则不会被关联到账号。
