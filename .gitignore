# ── 依赖 / 构建产物 ──
node_modules/
backend/node_modules/
helper/node_modules/
dist/
build/
out/
*.tsbuildinfo

# ── 运行数据（★ 必忽略：含加密凭据/解密盐/缓存，误提交会泄露邮箱账号） ──
backend/data/

# ── 凭据 / 密钥 ──
.env
.env.*
!.env.example
!**/.env.example
*.pem
*.key
*.p12
*.pfx
*.cred
.cred-salt

# ── 日志 / 调试 ──
*.log
*.debug.json
debug-*

# ── 编译产物（.NET 通知助手） ──
helper/bin/*.exe
helper/bin/*.dll
helper/**/bin/
helper/**/obj/

# ── IDE / 系统文件 ──
.vscode/
.idea/
*.iml
Thumbs.db
.DS_Store
desktop.ini
*.swp

# 模板文件已纳入版本控制（保持跟踪），故不在此忽略
# （此前 assets/plugin-page-template.html 出现在 ignore 中但已被 git 跟踪，
#  声明与行为矛盾，已移除该行）

# 项目文档（非运行必需）
CONTEXT.md
CURRENT.md
DECISIONS.md
PROJECT.md
PROJECT_LOG.md
STATUS.md
