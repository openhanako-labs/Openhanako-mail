# 当前状态

## 已完成
- 插件基础结构搭建（manifest / routes / tools / assets），已清理 React/Vite 残留
- `/mail` 页面路由可用，纸质档案室 UI 正常渲染
- 账号 CRUD 工具可用，用户通过 UI 自行添加
- 后端接入 `email-monitor` 真实能力，不再使用假数据
- 收件箱同步、列表、详情读写已真实打通
- 前端交互基本可用：账号管理、打开收件箱、同步、查看邮件

## 2026-07-25 重大更新（UI 重写）
- ✅ **邮件正文渲染修复**：详情改为独立页面，正文用**沙箱 iframe**(`sandbox="allow-popups"`) 渲染 `m.html.content`，不再 `esc()` 转义导致空白/乱码；`extractBody()` 兼容 `html`(string|{content})、`text`、`body`、`htmlBody` 多种形态（已用真实邮件验证字段）
- ✅ **侧边栏导航联动**：侧边栏新增账号切换器 + 动态文件夹列表（来自 `/folders` 缓存，回退默认 5 文件夹）；点击账号/文件夹联动列表刷新并高亮
- ✅ **发送/回复/转发 UI**：新增写邮件页面，`/send` 路由（POST，复用 `inbox.mjs` send/reply）；详情页「回复」「转发」带入原邮件上下文（收件人/主题/引用）
- ✅ **多账号 + 文件夹扩展**：多账号切换、默认文件夹（已发送/草稿/已删除/垃圾）列表与切换均已打通

## 待处理 / 待验证
- [前端需手动验证] 详情正文在 Hana 内 iframe 中正常显示（沙箱渲染）
- [前端需手动验证] 侧边栏账号/文件夹切换联动
- [前端需手动验证] 发送/回复/转发提交成功并回到列表
- 附件预览与下载（inbox.mjs 支持 downloadAttachment，前端未接）
- 本地加密存储（一期先走 dataDir）
- 搜索能力（inbox.mjs 支持 search）
- 列表/详情的「标记已读」按钮（后端 markRead 已存在，前端未接）
